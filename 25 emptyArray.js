let numbers = [1, 2, 3, 4];

// solution 1
// numbers = [];

// solution 2
numbers.length = 0;

// solution 3
// numbers.splice(0);

//solution 4 (not recommanded)
// while (numbers.length > 0) {
//   numbers.pop();
// }

console.log(numbers);
