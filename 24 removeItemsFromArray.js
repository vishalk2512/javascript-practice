const numbers = [1, 2, 3, 4, 5, 6, 7];

// remove item at the end
numbers.pop();

// remove item at the start
numbers.shift();

// remove item at the middle
numbers.splice(1, 3);

console.log(numbers);
